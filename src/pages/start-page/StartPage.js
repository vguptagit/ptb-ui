import  config  from "../../config/Config";

const Start = () => {
    var url = config.url.API_URL;
    console.log(url);
    return (
        <>
            Start
        </>
    );
}
export default Start;